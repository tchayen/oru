---
import Terminal from "./ui/Terminal.astro";
---

<section class="local-first">
  <div class="container local-grid">
    <div class="local-text">
      <div class="reveal">
        <p class="section-label">Local-first</p>
        <h2>Your data never leaves your machine.</h2>
        <p class="body-lg local-body">
          ao stores everything in a single SQLite database at
          <code>~/.ao/ao.db</code>. No accounts. No cloud. No third-party
          servers between you and your tasks.
        </p>
        <p class="body local-body-secondary">
          This is a deliberate architecture choice, not a limitation. Your task
          data is yours — readable, portable, queryable with any SQLite client.
        </p>
      </div>

      <div class="local-install reveal" data-delay="160">
        <h4>No sign-up. No login. No tokens.</h4>
        <p class="body-sm local-install-desc">
          Install it. Use it. There is no onboarding flow, no email field, no
          OAuth redirect. The database is created on first run. That is the
          entire setup.
        </p>
        <Terminal title="getting started">
          <span class="line"><span class="prompt">$ </span><span class="cmd">npm</span> <span class="sub">install</span> <span class="flag">-g</span> <span class="val">ao</span></span>
          <span class="line"><span class="prompt">$ </span><span class="cmd">ao</span> <span class="sub">add</span> <span class="str">"First task"</span></span>
          <span class="line"><span class="output">Created task 019414a5</span></span>
        </Terminal>
        <p class="body-sm local-footnote">
          Two commands from install to first task.
        </p>
      </div>
    </div>

    <div class="local-diagram reveal" data-delay="240">
      <div class="diagram">
        <div class="diagram-node diagram-cli">
          <div class="diagram-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="4 17 10 11 4 5"></polyline>
              <line x1="12" y1="19" x2="20" y2="19"></line>
            </svg>
          </div>
          <span class="diagram-label">CLI</span>
          <span class="diagram-sublabel">ao commands</span>
        </div>
        <div class="diagram-connector">
          <div class="diagram-line"></div>
        </div>
        <div class="diagram-node diagram-db">
          <div class="diagram-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <ellipse cx="12" cy="5" rx="9" ry="3"></ellipse>
              <path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"></path>
              <path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"></path>
            </svg>
          </div>
          <span class="diagram-label">SQLite</span>
          <span class="diagram-sublabel">~/.ao/ao.db</span>
        </div>
        <div class="diagram-connector">
          <div class="diagram-line"></div>
        </div>
        <div class="diagram-node diagram-server">
          <div class="diagram-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="2" width="20" height="8" rx="2" ry="2"></rect>
              <rect x="2" y="14" width="20" height="8" rx="2" ry="2"></rect>
              <line x1="6" y1="6" x2="6.01" y2="6"></line>
              <line x1="6" y1="18" x2="6.01" y2="18"></line>
            </svg>
          </div>
          <span class="diagram-label">ao server</span>
          <span class="diagram-sublabel">Local HTTP</span>
        </div>
        <div class="diagram-connector">
          <div class="diagram-line"></div>
        </div>
        <div class="diagram-node diagram-mobile">
          <div class="diagram-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
              <line x1="12" y1="18" x2="12.01" y2="18"></line>
            </svg>
          </div>
          <span class="diagram-label">iOS App</span>
          <span class="diagram-sublabel">Native companion</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .local-first {
    padding: 96px 0;
    background: var(--bg-elevated);
  }

  .local-grid {
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 64px;
    align-items: start;
  }

  .local-text {
    min-width: 0;
  }

  .local-text h2 {
    margin-bottom: 16px;
  }

  .local-body {
    color: var(--text-secondary);
    margin-bottom: 16px;
  }

  .local-body code {
    font-family: "JetBrains Mono", monospace;
    font-size: 14px;
    background: var(--bg-surface);
    padding: 2px 6px;
    border-radius: 4px;
    color: var(--accent);
  }

  .local-body-secondary {
    color: var(--text-tertiary);
    margin-bottom: 0;
  }

  .local-install {
    margin-top: 48px;
  }

  .local-install h4 {
    margin-bottom: 8px;
  }

  .local-install-desc {
    color: var(--text-tertiary);
    margin-bottom: 16px;
  }

  .local-footnote {
    color: var(--text-tertiary);
    margin-top: 12px;
    font-style: italic;
  }

  /* Diagram */
  .local-diagram {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 40px 0;
  }

  .diagram {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    width: 100%;
    max-width: 280px;
  }

  .diagram-node {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 24px 32px;
    background: var(--bg-surface);
    border: 1px solid var(--border-default);
    border-radius: 12px;
    width: 100%;
    text-align: center;
    transition: border-color 300ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .diagram-node:hover {
    border-color: var(--border-hover);
  }

  .diagram-db {
    border-color: var(--accent-border);
    box-shadow: 0 0 32px rgba(245, 158, 11, 0.08);
    animation: db-pulse 3s ease-in-out infinite;
  }

  .diagram-db:hover {
    border-color: var(--accent);
  }

  @keyframes db-pulse {
    0%, 100% {
      box-shadow: 0 0 32px rgba(245, 158, 11, 0.08);
    }
    50% {
      box-shadow: 0 0 40px rgba(245, 158, 11, 0.16);
    }
  }

  .diagram-icon {
    color: var(--text-tertiary);
    margin-bottom: 4px;
  }

  .diagram-db .diagram-icon {
    color: var(--accent);
  }

  .diagram-label {
    font-size: 14px;
    font-weight: 600;
    color: var(--text-primary);
  }

  .diagram-sublabel {
    font-size: 12px;
    color: var(--text-tertiary);
    font-family: "JetBrains Mono", monospace;
  }

  .diagram-connector {
    display: flex;
    justify-content: center;
    height: 32px;
  }

  .diagram-line {
    width: 1px;
    height: 100%;
    background: var(--border-default);
  }

  @media (prefers-reduced-motion: reduce) {
    .diagram-db {
      animation: none;
    }
  }

  @media (max-width: 767px) {
    .local-grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .local-diagram {
      order: -1;
    }
  }
</style>
