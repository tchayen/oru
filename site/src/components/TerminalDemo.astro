---
import Terminal from "./ui/Terminal.astro";
---

<section class="terminal-demo">
  <div class="container">
    <div class="terminal-demo-header reveal">
      <p class="section-label">Speed</p>
      <h2>See it work.</h2>
      <p class="body-lg terminal-demo-body">
        A task manager should be fast enough to disappear. ao runs against a
        local SQLite database with WAL mode. Every operation is a single-digit
        millisecond write. No spinners. No loading states. No network requests
        between you and your tasks.
      </p>
    </div>
    <div class="terminal-wrapper reveal" data-delay="160" data-terminal-typing>
      <Terminal title="~/.ao">
        <span class="line"><span class="prompt">$ </span><span class="cmd">ao</span> <span class="sub">add</span> <span class="str">"Refactor auth middleware"</span> <span class="flag">--priority</span> <span class="val">high</span> <span class="flag">--due</span> <span class="val">friday</span></span>
        <span class="line"><span class="output">Created task 019414a2</span></span>
        <span class="blank"></span>
        <span class="line"><span class="prompt">$ </span><span class="cmd">ao</span> <span class="sub">list</span></span>
        <span class="line"><span class="output">  #  Title                     Priority  Due        Status</span></span>
        <span class="line"><span class="output">  1  Refactor auth middleware   </span><span class="val">high</span><span class="output">      Fri Feb 20 todo</span></span>
        <span class="line"><span class="output">  2  Write migration tests     </span><span class="val">medium</span><span class="output">    —          in-progress</span></span>
        <span class="line"><span class="output">  3  Update API docs           </span><span class="val">low</span><span class="output">       —          todo</span></span>
        <span class="blank"></span>
        <span class="line"><span class="prompt">$ </span><span class="cmd">ao</span> <span class="sub">update</span> <span class="val">019414a2</span> <span class="flag">--status</span> <span class="val">done</span></span>
        <span class="line"><span class="output">Updated task 019414a2</span></span>
      </Terminal>
    </div>
  </div>
  <div class="terminal-demo-fade"></div>
</section>

<style>
  .terminal-demo {
    position: relative;
    padding: 0 0 96px;
    background: var(--bg-base);
  }

  .terminal-demo-header {
    text-align: center;
    margin-bottom: 48px;
  }

  .terminal-demo-header h2 {
    margin-bottom: 16px;
  }

  .terminal-demo-body {
    max-width: 640px;
    margin: 0 auto;
    color: var(--text-secondary);
  }

  .terminal-wrapper {
    max-width: 720px;
    margin: 0 auto;
  }

  .terminal-demo-fade {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 96px;
    background: linear-gradient(to bottom, transparent, var(--bg-base));
    pointer-events: none;
  }
</style>
