---
import InstallBlock from "./ui/InstallBlock.astro";
---

<section class="install-cta">
  <div class="cta-glow"></div>
  <div class="container cta-inner">
    <div class="reveal">
      <h2>Get started in one command.</h2>
    </div>
    <div class="cta-install reveal" data-delay="80">
      <InstallBlock />
    </div>
    <p class="body-sm cta-req reveal" data-delay="160">
      Requires Node 22+. That is the only dependency.
    </p>
    <div class="cta-links reveal" data-delay="240">
      <a href="https://github.com/AO_REPO" class="cta-link" target="_blank" rel="noopener noreferrer">GitHub</a>
      <span class="cta-sep">&middot;</span>
      <a href="https://www.npmjs.com/package/ao" class="cta-link" target="_blank" rel="noopener noreferrer">npm</a>
    </div>
    <p class="body-lg cta-closing reveal" data-delay="320">
      Your data. Your machine. Your agents. Your tasks.
    </p>
  </div>
</section>

<style>
  .install-cta {
    position: relative;
    padding: 128px 0 96px;
    background: var(--bg-base);
    overflow: hidden;
  }

  .cta-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 400px;
    background: radial-gradient(
      ellipse at center,
      rgba(245, 158, 11, 0.05) 0%,
      rgba(245, 158, 11, 0.02) 40%,
      transparent 70%
    );
    pointer-events: none;
  }

  .cta-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .cta-inner h2 {
    margin-bottom: 32px;
  }

  .cta-install {
    margin-bottom: 16px;
  }

  .cta-req {
    color: var(--text-tertiary);
    margin-bottom: 32px;
  }

  .cta-links {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 48px;
  }

  .cta-link {
    font-size: 14px;
    color: var(--text-tertiary);
    transition: color 200ms cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .cta-link:hover {
    color: var(--text-primary);
  }

  .cta-sep {
    color: var(--text-muted);
  }

  .cta-closing {
    color: var(--text-secondary);
    max-width: 480px;
  }
</style>
