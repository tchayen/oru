{
  "name": "oru-telemetry-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute oru-telemetry --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "wrangler": "^4.0.0"
  }
}
