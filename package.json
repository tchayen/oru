{
  "name": "ao-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "cli",
    "app"
  ],
  "type": "module",
  "scripts": {
    "test": "npm test --workspace=cli",
    "build": "npm run build --workspace=cli",
    "dev": "npm run dev --workspace=cli",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "tsgo": "npm run tsgo --workspace=cli"
  },
  "devDependencies": {
    "oxfmt": "^0.32.0",
    "oxlint": "^1.47.0"
  },
  "engines": {
    "node": ">=22"
  }
}
