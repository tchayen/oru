# Tasks

## Up next

- [x] Sort list output by priority (urgent > high > medium > low)
- [ ] End-to-end sync test across two separate DBs (simulate two devices)

## Ideas

- Bulk operations (`ao done --label shopping`, `ao delete --label old`)
- `ao done <id>` shortcut for `ao update <id> --status done`
- `ao labels` command to list all labels in use
